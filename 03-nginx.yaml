- name: install and run nginx
  hosts: web
  become: yes # -b
  tasks:
  - name: inatall ngnix
    ansible.builtin.package:
      name: ngnix
      state: present
  - name: start the nginx
    ansible.builtin.service:
     name: nginx
     state: started
				  